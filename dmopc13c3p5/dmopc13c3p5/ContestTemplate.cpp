// ContestTemplate.cpp : This file contains the 'main' function. Program execution begins and ends there.
//

#include <iostream>
#include <bits/stdc++.h>
#include <unordered_set>
using namespace std;
const int MM = 303, MU = 103, MR = 153;
#define ll long long
#define loop(r, x) for (int r = 0; r < x; r++)
typedef pair <int, int> pi;

int va[MM], ti[MU], fi[MR], dp[MM][MU][MR];
int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    int m, u, r;
    cin >> m >> u >> r;

    for (int i = 0; i < r; i++) {
        int a, b, c; 
        cin >> a >> b >> c;

    }
    return 0;
}

